internal/handlers/ (Controller / Handler)

Fungsi: Tempat file untuk menerima request HTTP dari client (misal: mengambil JSON, validasi data awal), lalu memanggil service/repository, dan mengembalikan response (JSON) kembali ke client.